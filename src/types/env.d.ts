// Environment type declarations for @env module.
// ANTHROPIC_API_KEY is intentionally absent — the mobile app
// never reads or stores an Anthropic key. All vision analysis
// goes through the Supabase analyze-scan Edge Function.
